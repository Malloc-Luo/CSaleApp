#include "HomePage.h"

int HomePage(void)
{
    int cmd = 0;

    cmd = HomePage_windows(cmd);

    cmd = HomePage_deal(cmd);

    cmd = HomePage_callback(cmd);

    return cmd;
}


int HomePage_windows(int cmd)
{

}

int HomePage_deal(int cmd)
{


}

int HomePage_callback(int cmd)
{

}












